tests/performance/.benchmark-regression-temp.db*

# Runtime state (not source)
.swarm/
.ruflo-state/
:memory:
agentops/

# CI benchmark artifacts (uploaded as workflow artifacts; not tracked)
benchmarks/ci-latest.json

# Coverage reports (generated; never tracked — see Task 5.1)
coverage/

# Secrets / local env — never commit
.env
.env.local
.env.*.local

# OS / editor noise
.DS_Store
*.log
# Local benchmark runs (WI-023): only baseline.json is tracked, refreshed
# deliberately via `npm run benchmark -- --update-baseline`.
benchmarks/results-*.json
