/**
 * calibration — measuring instruments + the gate that decides whether a score
 * may be labeled 'calibrated'. See gate.ts for the honesty mechanism.
 */

export * from './metrics';
export * from './gate';
